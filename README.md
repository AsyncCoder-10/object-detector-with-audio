# object-detector-with-audio
Object detection app using Hugging Face's DETR model with spoken audio descriptions via MMS-TTS, wrapped in a Gradio UI.


Create venv. Activate the venv. Install the requirements. Run the project. Then open the project in "http://127.0.0.1:7860".
